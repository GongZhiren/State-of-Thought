name: Release checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Install package and test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-tested.txt
          python -m pip install --no-deps -e .
      - name: Validate release identities and metrics
        run: make check
      - name: Run unit tests
        run: |
          status=0
          PYTHONPATH=src python -m pytest -q --junitxml=pytest.xml || status=$?
          if [ "$status" -ne 0 ]; then
            python scripts/report_pytest_failures.py pytest.xml
            exit "$status"
          fi
      - name: Build source and wheel archives
        run: python -m build
      - name: Smoke-test the built wheel
        run: |
          python -m pip uninstall -y state-of-thought
          python -m pip install --no-deps dist/state_of_thought-*.whl
          cd /tmp
          sot --help
          python -c "import sot; assert sot.__version__ == '1.0.0'"

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: package
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          lfs: true
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
      - name: Build release archives
        run: |
          python -m pip install build==1.6.0
          python -m build
          cd dist
          sha256sum state_of_thought-* > SHA256SUMS
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "${GITHUB_REF_NAME}" dist/*
          --verify-tag
          --title "State-of-Thought ${GITHUB_REF_NAME}"
          --notes-file CHANGELOG.md
